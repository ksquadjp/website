---
import "../styles/post.css";

import Head from "../components/Head.astro";
import Header from "../components/Header.tsx";
import Footer from "../components/Footer.astro";

const { title, description, heroImage } = Astro.props;
const publishedAt = new Date(Astro.props.publishedAt);
---

<html lang="ja">
  <Head title={title} description={description} />

  <body>
    <Header />
    <main class="mx-6 lg:mx-48">
      <article>
        <div class="py-24">
          {heroImage && <img width={1020} height={510} src={heroImage.url} alt="" />}
        </div>
        <div class="prose">
          <div class="title">
            <div class="date">
              <time datetime={publishedAt.toISOString()}>
                {
                  publishedAt.toLocaleDateString("ja-jp", {
                    year: "numeric",
                    month: "short",
                    day: "numeric",
                  })
                }
              </time>
            </div>
            <h1>{title}</h1>
            <hr />
          </div>
          <slot />
        </div>
      </article>
    </main>
    <Footer />
  </body>
</html>
